- `Optional` 객체는 **`null`** 을 안전하게 다루게 해주는 객체입니다.
    `null` 이란?
    - `null`은 프로그래밍에서 **값이 없음** 또는 **참조하지 않음** 을 나타내는 키워드
    - Optional 이 왜 필요한가? -  예시를 통해 null 이 무엇인지 실습해보세요.
    
- **`null`** 을 직접 다루는 대신 `Optional` 을 사용하면 `NullPointerException` 을 방지할 수 있습니다.

